# 轮趣四轮差速机器人详细信息表 V3.1

> 用于快速查询硬件、网络、外参、节点、话题、服务、启动文件、参数和验收值。
> 本表于 2026-08-31 按当前 `wheeltec_stack`、FAST-LIO2、Livox、Orbbec 和轮趣底盘节点源码及实机话题反查；不把厂家包中未被正式入口加载的演示节点列入运行链。

## 1. 硬件

| 字段 | 当前值 |
|---|---|
| 车型 | 轮趣四轮差速 |
| 尺寸 | 长 0.50 m、宽 0.40 m |
| 主控 | NVIDIA Jetson NX，aarch64 |
| 系统 | Ubuntu 20.04 |
| ROS | Noetic |
| 用户 | `<ROS_USER>` |
| 工作空间 | `~/livox_fastlio` |
| 雷达 | Livox Mid-360 |
| 雷达 SN | `<LIVOX_SN>` |
| RGB/Depth | Orbbec Gemini 336L，USB ID `2bc5:0807`，USB 3.2 |
| 底盘接口 | USB 串口 |
| 设备链接 | /dev/wheeltec_controller→ttyACM0 |

## 2. 网络

| 对象 | 地址 | 备注 |
|---|---|---|
| NX wlan0 | `<NX_MANAGEMENT_IP>/24` | SSH、默认路由 |
| wlan0 网关 | `<NX_MANAGEMENT_GATEWAY>` | 默认路由 |
| NX eth0 | 192.168.1.5/24 | 雷达主机地址 |
| NX eth0 辅助 | `<AUX_ETH_IP>/24` | 保留设备网段 |
| Mid-360 | 192.168.1.165 | 序列号后两位 65 |
| eth0 路由 | never-default | 不抢互联网默认路由 |

## 3. 外参与 TF

base_link→body：

| x | y | z | roll | pitch | yaw |
|---:|---:|---:|---:|---:|---:|
| +0.10 m | 0 | +0.15 m | 0° | +20° | 0° |

坐标约定：前、左、上。

| TF | 类型 | 唯一发布者 |
|---|---|---|
| map→odom | 动态 | wheeltec_global_localizer |
| odom→camera_init | 静态 | wheeltec_geometry_tf_publisher |
| camera_init→body | 动态 | FAST-LIO2 |
| body→base_link | 静态 | wheeltec_tf_manager |
| base_link→camera_link | 静态 | wheeltec_camera_mount_tf；平移 `[0.16,0,0.08] m`，RPY `[0,0,0] rad` |
| camera_link→camera_*_optical_frame | 静态 | Orbbec 驱动 |

实车验证：约 0.26 m 前进/倒车闭环后 Z 残差约 1.6 mm，保留 Pitch +20°。

## 4. 正式入口

| 模式 | 命令 | 说明 |
|---|---|---|
| 建图 | `roslaunch wheeltec_system_bringup wheeltec_mapping.launch map_name:=NAME` | Livox、FAST-LIO、mapper、TF、pose adapter、底盘 |
| 地图最终生成 | `rosrun wheeltec_map_tools finalize_map.py NAME` | 读取 mapper 保存的 PCD，生成定位与导航地图资产 |
| 定位 | `roslaunch wheeltec_system_bringup wheeltec_localization.launch map_name:=NAME` | Livox、FAST-LIO、TF、NDT、两套地图、底盘 |
| TEB 导航 | `roslaunch wheeltec_navigation navigation_teb.launch map_name:=NAME` | 正式导航入口；须先启动定位 |
| DWA 导航 | `roslaunch wheeltec_navigation navigation.launch map_name:=NAME` | 回退/对比入口；须先启动定位 |
| 全局规划安全测试 | `roslaunch wheeltec_navigation global_planning_test.launch map_name:=NAME` | `/cmd_vel` 被重映射到阻断话题，不驱动车辆 |
| 导航日志 | `roslaunch wheeltec_navigation nav_logging.launch tag:=TAG` | 定位和导航启动后运行，Ctrl+C 完成 bag 与分析 |
| Livox+底盘基础链路 | `roslaunch wheeltec_system_bringup wheeltec_livox_base.launch` | 只验证雷达驱动和串口底盘 |
| FAST-LIO+TF+底盘 | `roslaunch wheeltec_system_bringup wheeltec_system.launch` | 系统诊断入口，不含 NDT 和地图服务器 |
| Gemini 336L | `roslaunch wheeltec_system_bringup wheeltec_orbbec336l.launch` | RGB、深度、点云、相机内部 TF 和安装 TF；不启动底盘 |

禁止同时启动 mapping 和 localization，禁止重复启动 Livox 或底盘节点。

## 5. 节点

模式缩写：M=建图，L=定位，N=导航，T=全局规划安全测试，O=按需工具。

| 节点 | 模式 | 主要输入 | 主要输出/职责 |
|---|---|---|---|
| `/livox_lidar_publisher2` | M/L | Mid-360 UDP 数据 | `/livox/lidar`、`/livox/imu` |
| `/laserMapping` | M/L | `/livox/lidar`、`/livox/imu` | `/Odometry`、两套注册点云、`camera_init→body` |
| `/wheeltec_robot` | M/L | `/cmd_vel`、`/dev/wheeltec_controller` | `/odom`、`/imu`、`/PowerVoltage`；不发布底盘 TF |
| `/wheeltec_pointcloud_mapper` | M | `/cloud_registered`、`/Odometry` | 静态扫描、静态地图、保存/清空服务、30 s 自动保存 |
| `/wheeltec_geometry_tf_publisher` | M/L | `/wheeltec_geometry` 参数 | 静态 `odom→camera_init` |
| `/wheeltec_tf_manager` | M/L | `extrinsics.yaml` | 静态 `body→base_link` |
| `/wheeltec_pose_adapter` | M/L | TF `odom→base_link` | `/fastlio_odom`，仅位姿，twist 固定为 0 |
| `/wheeltec_cloud_adapter` | L | `/cloud_registered_body`、TF | `/cloud_registered_base` |
| `/wheeltec_map_loader` | L | `public_map.pcd` | 锁存 `/map_cloud` |
| `/wheeltec_global_localizer` | L | `/map_cloud`、`/cloud_registered_base`、`/fastlio_odom`、`/initialpose` | 动态 `map→odom` |
| `/wheeltec_map_server` | L | `map.yaml` | `/map_2d`、`/map_metadata` |
| `/wheeltec_navigation_map_server` | N/T | `map_raw.yaml` | `/nav_static_map`、`/nav_static_map_metadata` |
| `/move_base` | N/T | 静态地图、TF、点云、`/odom`、目标 | action 状态、全局/局部路径、costmap、`/cmd_vel` |
| `/wheeltec_global_plan_tester` | T | `/clicked_point`、TF、`/move_base/make_plan` | `/wheeltec_global_plan_test` |
| `/camera/camera` | O | Gemini 336L USB 数据 | RGB、深度、点云、设备服务和相机内部 TF |
| `/wheeltec_camera_mount_tf` | O | 启动参数 | 静态 `base_link→camera_link`；x=0.16、y=0、z=0.08 m，RPY 为 0 |

`nav_log_session.sh` 是由 roslaunch 管理的记录进程，但脚本自身没有注册 ROS 节点，因此不能把 `/nav_log_session` 是否出现在 `rosnode list` 作为验收条件。它输出 LZ4 rosbag、CSV 和 `summary.txt`，不改变控制参数。

## 6. 话题

频率为当前配置下的期望值；实车验收以 `rostopic hz` 为准。“锁存”表示新订阅者连接后立即收到最后一帧。

### 6.1 雷达、FAST-LIO 与底盘

| 话题 | 类型 | 发布者 → 主要订阅者 | frame/频率/状态 | 用途 |
|---|---|---|---|---|
| `/livox/lidar` | `livox_ros_driver2/CustomMsg` | Livox → FAST-LIO | `livox_frame`，约 10 Hz | 原始 Livox 点云 |
| `/livox/imu` | `sensor_msgs/Imu` | Livox → FAST-LIO | 驱动时间戳，约 200 Hz | 雷达 IMU |
| `/Odometry` | `nav_msgs/Odometry` | FAST-LIO → mapper/诊断 | `camera_init`→`body`，约 10 Hz | FAST-LIO 原始位姿 |
| `/cloud_registered` | `sensor_msgs/PointCloud2` | FAST-LIO → mapper | `camera_init`，约 10 Hz | 世界系注册点云；建图输入 |
| `/cloud_registered_body` | `sensor_msgs/PointCloud2` | FAST-LIO → cloud adapter/local costmap | `body`，约 10 Hz | 车体系注册点云；导航障碍源 |
| `/path` | `nav_msgs/Path` | FAST-LIO → RViz/诊断 | 当前 `publish/path_en=false`，通常无消息 | FAST-LIO 轨迹；默认关闭 |
| `/cloud_effected` | `sensor_msgs/PointCloud2` | FAST-LIO | 发布调用在源码中关闭，通常无消息 | 有效特征点调试 |
| `/Laser_map` | `sensor_msgs/PointCloud2` | FAST-LIO | 发布调用在源码中关闭，通常无消息 | 局部激光地图调试 |
| `/odom` | `nav_msgs/Odometry` | 底盘 → move_base | header=`wheel_odom`，child=`base_footprint`，约 20 Hz | 编码器位姿与真实速度反馈 |
| `/imu` | `sensor_msgs/Imu` | 底盘 → 诊断 | `gyro_link`，约 20 Hz | 底盘板载 IMU |
| `/PowerVoltage` | `std_msgs/Float32` | 底盘 → 诊断 | 无 header，底盘周期 | 电池电压 |
| `/cmd_vel` | `geometry_msgs/Twist` | move_base/遥控 → 底盘 | 按控制周期 | 仅使用 `linear.x`、`angular.z` |

### 6.2 Orbbec Gemini 336L

驱动按需启流；没有订阅者时不持续传输图像属于正常行为。

| 话题 | 类型 | frame/实测频率 | 用途 |
|---|---|---|---|
| `/camera/color/image_raw` | `sensor_msgs/Image` | `camera_color_optical_frame`，30 Hz，1280×720 MJPG | RGB 原图 |
| `/camera/color/image_raw/compressed` | `sensor_msgs/CompressedImage` | `camera_color_optical_frame`，有订阅者时 | RGB 压缩传输 |
| `/camera/color/camera_info` | `sensor_msgs/CameraInfo` | `camera_color_optical_frame`，随 RGB | RGB 内参 |
| `/camera/color/metadata` | `orbbec_camera/Metadata` | 随 RGB | RGB 帧元数据 |
| `/camera/depth/image_raw` | `sensor_msgs/Image` | `camera_depth_optical_frame`，约 30 Hz，848×480 Y16 | 深度图 |
| `/camera/depth/camera_info` | `sensor_msgs/CameraInfo` | `camera_depth_optical_frame`，随深度 | 深度内参 |
| `/camera/depth/points` | `sensor_msgs/PointCloud2` | `camera_depth_optical_frame`，约 30 Hz | 无颜色深度点云 |
| `/camera/depth/metadata` | `orbbec_camera/Metadata` | 随深度 | 深度帧元数据 |
| `/camera/depth_filters/status` | `orbbec_camera/DepthFiltersStatus` | 状态周期 | 深度滤波器开关与参数状态 |
| `/camera/depth_to_color` | `orbbec_camera/Extrinsics` | 初始化/配置变化 | 深度到彩色相机的内部外参 |
| `/camera/device_status` | `orbbec_camera/DeviceStatus` | 设备状态周期 | 在线状态与异常诊断 |
| `/camera/sdk_version` | `std_msgs/String` | 设备初始化后发布 | SDK/Wrapper 版本 |
| `/diagnostics` | `diagnostic_msgs/DiagnosticArray` | 默认 1 Hz | 相机和其他 ROS 节点共享的诊断总线 |

实测设备为 Gemini 336L，序列号 `<ORBBEC_SN>`，固件 `1.4.60`，SDK/Wrapper `2.9.3`。正式 `base_link→camera_link` 为前 0.16 m、左 0 m、上 0.08 m，RPY 均为 0；相机与车体同姿态。

### 6.3 建图器

| 话题 | 类型 | 发布者 → 订阅者 | frame/频率/状态 | 用途 |
|---|---|---|---|---|
| `/wheeltec/static_scan` | `sensor_msgs/PointCloud2` | mapper → RViz/诊断 | `camera_init`，随有效扫描，非锁存 | 本帧判定为静态的点 |
| `/wheeltec/static_map_cloud` | `sensor_msgs/PointCloud2` | mapper → RViz/诊断 | `camera_init`，每 2 s，锁存 | 当前内存静态地图 |
| `/wheeltec/dynamic_points` | `sensor_msgs/PointCloud2` | mapper → RViz/诊断 | 当前 `publish_dynamic_points=false`，话题不创建 | 被判为动态/候选的点 |

### 6.4 定位、地图与 TF

| 话题 | 类型 | 发布者 → 主要订阅者 | frame/频率/状态 | 用途 |
|---|---|---|---|---|
| `/fastlio_odom` | `nav_msgs/Odometry` | pose adapter → NDT | `odom`→`base_link`，默认 20 Hz；twist=0 | NDT 位姿预测，不能作 TEB 速度反馈 |
| `/cloud_registered_base` | `sensor_msgs/PointCloud2` | cloud adapter → NDT | `base_link`，约 10 Hz | NDT 当前扫描 |
| `/map_cloud` | `sensor_msgs/PointCloud2` | map loader → NDT | `map`，锁存 | NDT 三维参考地图 |
| `/map_2d` | `nav_msgs/OccupancyGrid` | 定位 map_server → RViz | `map`，锁存 | 便于观察的二维地图 |
| `/map_metadata` | `nav_msgs/MapMetaData` | 定位 map_server | 锁存 | `/map_2d` 元数据 |
| `/nav_static_map` | `nav_msgs/OccupancyGrid` | 导航 map_server → global costmap | `map`，锁存 | 导航实际加载的 `map_raw.yaml` |
| `/nav_static_map_metadata` | `nav_msgs/MapMetaData` | 导航 map_server | 锁存 | 导航原始地图元数据 |
| `/initialpose` | `geometry_msgs/PoseWithCovarianceStamped` | RViz/用户 → NDT | `map`，事件触发 | NDT 初始位姿；不是 move_base 目标 |
| `/tf` | `tf2_msgs/TFMessage` | FAST-LIO、NDT → 全系统 | 动态 | `camera_init→body`、`map→odom` 等动态 TF |
| `/tf_static` | `tf2_msgs/TFMessage` | 两个 TF 管理节点 → 全系统 | 锁存 | `odom→camera_init`、`body→base_link` |

NDT 源码内部名称 `/velodyne_points`、`/odom_lio` 已在 `wheeltec_relocalization.launch` 中分别重映射到 `/cloud_registered_base`、`/fastlio_odom`，正常运行图中不应再要求原名称存在。

### 6.5 move_base 控制、action 与路径

| 话题 | 类型 | 方向 | 用途 |
|---|---|---|---|
| `/move_base_simple/goal` | `geometry_msgs/PoseStamped` | RViz → move_base | RViz“2D Nav Goal”简单目标入口 |
| `/move_base/goal` | `move_base_msgs/MoveBaseActionGoal` | action 客户端 → move_base | 完整 action 目标 |
| `/move_base/cancel` | `actionlib_msgs/GoalID` | action 客户端 → move_base | 取消目标 |
| `/move_base/status` | `actionlib_msgs/GoalStatusArray` | move_base → 客户端 | PENDING/ACTIVE/SUCCEEDED/ABORTED 等状态 |
| `/move_base/feedback` | `move_base_msgs/MoveBaseActionFeedback` | move_base → 客户端 | 当前机器人位姿反馈 |
| `/move_base/result` | `move_base_msgs/MoveBaseActionResult` | move_base → 客户端 | 目标最终结果 |
| `/move_base/current_goal` | `geometry_msgs/PoseStamped` | move_base → 诊断 | move_base 当前采用的目标 |
| `/move_base/GlobalPlanner/plan` | `nav_msgs/Path` | GlobalPlanner → TEB/RViz | 全局规划路径 |
| `/move_base/TebLocalPlannerROS/global_plan` | `nav_msgs/Path` | TEB → RViz/日志 | TEB 截取后的全局路径 |
| `/move_base/TebLocalPlannerROS/local_plan` | `nav_msgs/Path` | TEB → RViz/日志 | 当前局部轨迹；有全局路径但该话题空时重点查 TEB |
| `/move_base/TebLocalPlannerROS/teb_poses` | `geometry_msgs/PoseArray` | TEB → RViz/日志 | 优化轨迹离散位姿 |
| `/move_base/TebLocalPlannerROS/teb_markers` | `visualization_msgs/Marker` | TEB → RViz/日志 | TEB 可视化标记 |
| `/move_base/TebLocalPlannerROS/teb_feedback` | `teb_local_planner/FeedbackMsg` | TEB → 日志 | 轨迹、速度和障碍诊断；当前已启用 |
| `/move_base/TebLocalPlannerROS/obstacles` | `costmap_converter/ObstacleArrayMsg` | 外部障碍源 → TEB | 可选自定义障碍输入；当前没有发布者，TEB 主要读取 costmap |
| `/move_base/TebLocalPlannerROS/via_points` | `nav_msgs/Path` | 外部节点 → TEB | 可选 via points 输入；当前没有发布者且约束关闭 |
| `/wheeltec_navigation/cmd_vel_blocked` | `geometry_msgs/Twist` | 全局规划测试 move_base → 无底盘订阅者 | 安全测试专用阻断输出 |
| `/clicked_point` | `geometry_msgs/PointStamped` | RViz → global plan tester | “Publish Point”触发仅规划测试 |
| `/wheeltec_global_plan_test` | `nav_msgs/Path` | global plan tester → RViz | `/move_base/make_plan` 的锁存测试结果 |

#### DWA 回退入口专用话题

| 话题 | 类型 | 状态/用途 |
|---|---|---|
| `/move_base/DWAPlannerROS/global_plan` | `nav_msgs/Path` | DWA 接收/裁剪后的全局路径 |
| `/move_base/DWAPlannerROS/local_plan` | `nav_msgs/Path` | DWA 局部轨迹 |
| `/move_base/DWAPlannerROS/trajectory_cloud` | `sensor_msgs/PointCloud2` | DWA 采样轨迹评分调试 |
| `/move_base/DWAPlannerROS/cost_cloud` | `sensor_msgs/PointCloud2` | DWA 代价调试 |
| `/move_base/DWAPlannerROS/parameter_updates` | `dynamic_reconfigure/Config` | DWA 动态参数变化 |

正式 TEB 入口不要求上述 DWA 话题有消息。

### 6.6 costmap 与动态参数诊断

| 话题族 | 类型 | 说明 |
|---|---|---|
| `/move_base/global_costmap/costmap` | `nav_msgs/OccupancyGrid` | 全局代价地图，配置发布频率 1 Hz |
| `/move_base/local_costmap/costmap` | `nav_msgs/OccupancyGrid` | 6 m×6 m 局部代价地图，配置发布频率 4 Hz |
| `/move_base/*_costmap/costmap_updates` | `map_msgs/OccupancyGridUpdate` | costmap 增量更新；是否持续有消息取决于更新情况 |
| `/move_base/*_costmap/footprint` | `geometry_msgs/PolygonStamped` | 当前机器人 footprint |
| `/move_base/parameter_updates` | `dynamic_reconfigure/Config` | move_base 动态参数变化 |
| `/move_base/TebLocalPlannerROS/parameter_updates` | `dynamic_reconfigure/Config` | TEB 动态参数变化 |
| `/move_base/*_costmap/*_layer/parameter_updates` | `dynamic_reconfigure/Config` | obstacle/inflation/static layer 参数变化 |
| 上述命名空间下的 `parameter_descriptions` | `dynamic_reconfigure/ConfigDescription` | 对应节点/插件可动态调整的参数描述，通常锁存 |
| `/rosout`、`/rosout_agg` | `rosgraph_msgs/Log` | 节点日志与聚合日志；导航记录脚本保存 `/rosout_agg` |

## 7. 服务和地图

| 名称 | 类型 | 提供者/用途 |
|---|---|---|
| `/wheeltec_pointcloud_mapper/save_map` | `std_srvs/Trigger` | mapper：立即保存当前过滤 PCD；不是完整 finalize |
| `/wheeltec_pointcloud_mapper/reset_map` | `std_srvs/Empty` | mapper：清空内存地图，谨慎使用 |
| `/move_base/make_plan` | `nav_msgs/GetPlan` | move_base：只请求全局路径，不执行运动 |
| `/move_base/clear_costmaps` | `std_srvs/Empty` | move_base：清除可清除的 costmap 层 |
| `/static_map` | `nav_msgs/GetMap` | map_server 标准服务；定位和导航同时存在两个 map_server 时不要靠此同名服务区分地图，应直接订阅 `/map_2d` 或 `/nav_static_map` |
| `/move_base/set_parameters` | `dynamic_reconfigure/Reconfigure` | 动态修改 move_base 参数 |
| `/move_base/TebLocalPlannerROS/set_parameters` | `dynamic_reconfigure/Reconfigure` | 动态修改 TEB 参数 |
| `/move_base/*_costmap/*_layer/set_parameters` | `dynamic_reconfigure/Reconfigure` | 动态修改 costmap 各层参数 |
| `/camera/get_device_info` | `orbbec_camera/GetDeviceInfo` | 返回型号、序列号、固件和硬件信息；已实测成功 |
| `/camera/get_sdk_version` | `orbbec_camera/GetString` | 返回固件、Orbbec SDK 和 ROS Wrapper 版本 |
| `/camera/get_color_camera_info`、`/camera/get_depth_camera_info` | `orbbec_camera/GetCameraInfo` | 按服务读取 RGB/深度内参 |
| `/camera/toggle_color`、`/camera/toggle_depth` | `orbbec_camera/SetBool` | 运行时开关 RGB/深度流 |
| `/camera/save_images`、`/camera/save_point_cloud` | `std_srvs/Empty` | 让驱动保存一帧图像或点云；不用于地图保存 |
| `/camera/get_*` | Orbbec 自定义服务 | 查询曝光、增益、白平衡、激光、LDP、点云抽稀及设备配置 |
| `/camera/set_*`、`/camera/reset_*` | Orbbec 自定义服务 | 修改/复位曝光、增益、滤波、镜像、旋转、流配置等；调参前记录原值 |
| `/camera/reboot_device` | `std_srvs/Empty` | 重启相机设备，会短时中断全部相机话题，谨慎使用 |

地图目录：

~~~text
~/livox_fastlio/maps/NAME/
├─ filtered_camera_init.pcd
├─ raw_camera_init.pcd
├─ public_map.pcd
├─ map_raw.pgm
├─ map_raw.yaml
├─ map.pgm
├─ map.yaml
└─ map_metadata.yaml
~~~

## 8. 导航参数

| 参数 | 值 |
|---|---:|
| footprint | ±0.25 m×±0.20 m |
| padding | 0.03 m |
| max_vel_x | 0.35 m/s |
| max_vel_x_backwards | 0.15 m/s |
| max_vel_y | 0 |
| max_vel_theta | 0.80 rad/s |
| acc_lim_x | 0.20 m/s² |
| acc_lim_theta | 0.30 rad/s² |
| xy / yaw goal tolerance | 0.15 m / 0.15 rad |
| TEB min_obstacle_dist / inflation_dist | 0.15 m / 0.35 m |
| TEB lookahead / prune distance | 3.0 m / 1.0 m |
| TEB internal oscillation recovery | 启用，最短 10.0 s |
| planner / controller frequency | 1.0 Hz / 10.0 Hz |
| planner / controller patience | 5.0 s / 5.0 s |
| recovery_behavior / clearing_rotation | false / false |
| move_base oscillation_timeout | 20.0 s |
| move_base oscillation_distance | 0.05 m |
| local costmap | 6 m×6 m |
| costmap resolution | 0.05 m |
| local costmap update / publish | 8.0 Hz / 4.0 Hz |
| global costmap update / publish | 2.0 Hz / 1.0 Hz |
| costmap transform_tolerance | 0.5 s |
| global/local inflation_radius | 0.45 m |
| global/local cost_scaling_factor | 4.0 |
| obstacle height | 0.08～1.50 m |
| obstacle range | 4.0 m |
| raytrace range | 5.0 m |

底盘资料物理上限：前后 0.50 m/s、角速度 1.50 rad/s。

## 9. 点云参数

| 参数 | 值 |
|---|---:|
| input range | 0.50～50.0 m |
| scan_voxel_size | 0.05 m |
| radius filter radius | 0.15 m |
| radius min_neighbors | 2 |
| dynamic voxel_size | 0.20 m |
| hit / miss probability | 0.70 / 0.40 |
| occupied / clearing probability | 0.72 / 0.35 |
| min_hit_scans / observation span | 8 / 2.0 s |
| ray stride / max range | 4 / 20.0 m |
| ray endpoint margin | 0.30 m |
| candidate timeout / cleanup period | 5.0 s / 1.0 s |
| dynamic max_voxels | 2,000,000 |
| map voxel_size | 0.05 m |
| map max_voxels | 5,000,000 |
| 自动保存 | 每 30 s；正常退出时保存 |
| 静态地图话题发布周期 | 2.0 s |
| 最大点云/里程计时间差 | 0.20 s |
| self_filter | false |
| self_filter x | -0.30～+0.30 m |
| self_filter y | -0.25～+0.25 m |
| self_filter z | -0.20～+0.20 m |

## 10. 验收值

| 检查 | 合格 |
|---|---|
| 雷达 | lidar 约 10 Hz，IMU 约 200 Hz |
| FAST-LIO | `/Odometry`、`/cloud_registered`、`/cloud_registered_body` 约 10 Hz |
| 底盘 | `/odom`、`/imu` 约 20 Hz，`/cmd_vel` 非零时 twist 同向响应 |
| Gemini 336L | RGB 30 Hz、深度约 30 Hz、点云约 30 Hz；设备与 SDK 服务成功 |
| mapper | 建图时 `/wheeltec/static_scan` 有数据、`/wheeltec/static_map_cloud` 每 2 s 更新 |
| TF | map→odom→camera_init→body→base_link 单链；相机启用时增加 base_link→camera_link→camera_*_optical_frame |
| 外参 | 直行 X 同向、Y 小、往返 Z 闭合 |
| NDT | `/map_cloud` 锁存、`/cloud_registered_base` 连续，点云与地图重合，map→odom 稳定 |
| 导航 | `/move_base/status` 正常、global/local plan 均有数据、`/cmd_vel` 不超过限制 |

## 11. 关键文件

| 目的 | 相对 src 的路径 |
|---|---|
| Livox IP | livox_ros_driver2/config/MID360_config.json |
| Orbbec 官方驱动 | OrbbecSDK_ROS1（`v2-main`，固定提交 `a2838b3`） |
| 相机一键入口与安装 TF | wheeltec_system_bringup/launch/wheeltec_orbbec336l.launch |
| Orbbec ARM64 链接补丁 | 工作空间根目录 third_party_patches/orbbec_ros1_aarch64_link_order.patch |
| FAST-LIO | fast_lio/config/mid360.yaml |
| 底盘启动 | turn_on_wheeltec_robot/launch/include/base_serial.launch |
| 外参 | wheeltec_tf_manager/config/extrinsics.yaml |
| 几何 | wheeltec_system_bringup/config/wheeltec_geometry.yaml |
| 建图入口 | wheeltec_system_bringup/launch/wheeltec_mapping.launch |
| 定位入口 | wheeltec_system_bringup/launch/wheeltec_localization.launch |
| NDT 入口与参数 | wheeltec_system_bringup/launch/wheeltec_relocalization.launch |
| mapper | wheeltec_pointcloud_mapper/config/mapper.yaml |
| footprint | wheeltec_navigation/config/costmap_common.yaml |
| global/local costmap | wheeltec_navigation/config/global_costmap.yaml、local_costmap.yaml |
| move_base 公共/TEB 参数 | wheeltec_navigation/config/move_base.yaml、move_base_teb.yaml |
| TEB | wheeltec_navigation/config/teb_local_planner.yaml |
| 导航入口 | wheeltec_navigation/launch/navigation_teb.launch |
| 全局规划安全测试 | wheeltec_navigation/launch/global_planning_test.launch、scripts/global_plan_tester.py |
| 导航日志 | wheeltec_navigation/launch/nav_logging.launch、scripts/nav_log_session.sh |

## 12. 故障矩阵

| 现象 | 诊断顺序 |
|---|---|
| 无雷达 | eth0→Ping→驱动节点→/livox/lidar |
| 无底盘 | lsusb→ttyACM0→udev→权限→节点 |
| 无相机设备 | `lsusb` 的 `2bc5:0807`→USB 3.0 数据线→接口/供电→udev 规则 |
| 相机节点存在但无数据 | 建立话题订阅→toggle 服务→USB 连接→节点日志；驱动默认按需启流 |
| 相机链接失败并出现 `cv::Mat` | OpenCV 头文件版本→运行库版本→统一为 4.2.0→重新编译相机目标 |
| 相机点云方向/位置错误 | base_link→camera_link 实测外参→相机是否松动→禁止覆盖 optical TF |
| FAST-LIO 无输出 | lidar→Livox IMU→初始化→配置 |
| TF 断开 | 缺边→发布者→时间→重复边 |
| NDT 不收敛 | 地图→frame→初值→外参→参数 |
| 有全局路径但无局部路径 | `/move_base/TebLocalPlannerROS/local_plan`→局部 costmap→障碍点云→TF→TEB 日志 |
| 有路径车不动 | `/move_base/status`→`/cmd_vel`→`/odom.twist`→串口→急停；若 ABORTED 且提示 oscillating，核对 20.0 s / 0.05 m |
| 地图不完整 | filtered_camera_init.pcd→finalize_map.py 输出→目录权限→磁盘 |
| 直行 Z 漂移 | Pitch→安装松动→地面→FAST-LIO 初始化 |

## 13. 运行时清单核验

不同入口出现的话题不同，不能用一份 `rostopic list` 同时要求建图、定位、导航话题全部存在。按当前启动模式执行：

~~~bash
rosnode list | sort
rostopic list -v
rosservice list | sort
rosparam get /move_base  # 仅导航模式
rosrun tf view_frames
~~~

核对单个接口时使用：

~~~bash
rostopic info /TOPIC
rostopic type /TOPIC
rostopic hz /TOPIC
rostopic echo -n 1 /TOPIC
rosservice type /SERVICE
~~~

`rostopic list` 中存在但 `rostopic hz` 没有数据，不一定是故障：本表已标明 `/path`、`/cloud_effected`、`/Laser_map` 等默认关闭的调试输出，Orbbec 图像和点云还会按订阅者需求启流。反之，`/wheeltec/dynamic_points` 在当前配置下不会被创建。
